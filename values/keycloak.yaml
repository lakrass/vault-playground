keycloak:
  httpsEnabled: false
  adminUser: admin
  adminPassword: admin

ingress:
  enabled: true
  className: nginx
  hosts:
    - host: localhost
      paths:
        - path: /
          pathType: Prefix

persistence:
  enabled: true
  size: 200Mi
