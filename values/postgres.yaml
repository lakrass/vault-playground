settings:
  superuser:
    value: postgres
  superuserPassword:
    value: postgres

userDatabase:
  name:
    value: keycloak
  user:
    value: keycloak
  password:
    value: keycloak

service:
  type: NodePort
  nodePort: 30432

storage:
  requestedSize: 200Mi

args:
  - -c
  - config_file=/etc/postgresql/postgresql.conf

customConfig: |
  listen_addresses = '*'
  logging_collector = on
  log_destination = 'jsonlog'
  log_statement = 'all'
  log_directory = '/tmp/'
